const initialState = {
  id: null,
  displayName: '',
};

export default (state = initialState, action) => {
  switch (action.type) {
    case ___:
      return null;

    default:
      return state;
  }
};
